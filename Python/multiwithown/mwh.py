# This code is asking the user to enter a number and then storing that number in the variable `val`.
# It then calculates the square of `val` and prints the result.
val = int(input("please enter your number: "))

print(f"your result is {val * val}")